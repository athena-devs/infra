**Infrastructure Repo**

This repo is meant to be a structural pillar for applications cloud based using cloudflared solution to reverse proxy instead nginx since it's configuration is way simpler all as my other repos is completely container-based and reproducible in any place thats support docker instances as well, beign easy to implement in any cloud platform.

I also have included n8n, since to my personal case is very useful to have a centralized instance that manage all my workflows turning it on a SPOF, that can be easierly maintained and updated 